<h1><%= @i.title %></h1>

<div class="container">
    <div class="row">
      <a href="http://localhost:3000/movies/edit_form/<%= @i.id %>">Edit</a>
      <a href="http://localhost:3000/movies/delete/<%= @i.id %>">Delete</a>
    <div class="row">
        <div class="col-sm-2"> <%= @i.year %></div>
        <div class="col-sm-2"> <%= @i.duration %></div>
        <div class="col-sm-6"> <img src="<%= @i.image_url %>"></div>
    </div>
    <div class="row">
        <div class="col-sm-10"> <%= @i.description %> </div>
    </div>
</div>
